info:
  name: Delete Registration
  type: http
  seq: 15

http:
  method: DELETE
  url: http://127.0.0.1:8000/api/tournaments/2/registrations/3
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
