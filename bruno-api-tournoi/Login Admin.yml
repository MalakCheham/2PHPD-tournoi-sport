info:
  name: Login Admin
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  body:
    type: json
    data: |-
      {
          "emailAddress": "admin@tournoi.com",
          "password": "admin123"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
