info:
  name: Admin - Get Registrations
  type: http
  seq: 26

http:
  method: GET
  url: http://127.0.0.1:8000/admin/registrations
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
