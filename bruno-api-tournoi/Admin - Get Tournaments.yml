info:
  name: Admin - Get Tournaments
  type: http
  seq: 25

http:
  method: GET
  url: http://127.0.0.1:8000/admin/tournaments
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
