info:
  name: Get Registration
  type: http
  seq: 13

http:
  method: GET
  url: http://127.0.0.1:8000/api/tournaments/2/registrations
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
