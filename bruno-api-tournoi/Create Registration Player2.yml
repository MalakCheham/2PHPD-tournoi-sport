info:
  name: Create Registration Player2
  type: http
  seq: 19

http:
  method: POST
  url: http://127.0.0.1:8000/api/tournaments/2/registrations
  body:
    type: json
    data: '{"playerId": 3}'
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
