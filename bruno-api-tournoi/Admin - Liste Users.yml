info:
  name: Admin - Liste Users
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8000/admin/users
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
