info:
  name: Create Sport Match
  type: http
  seq: 21

http:
  method: POST
  url: http://127.0.0.1:8000/api/tournaments/2/sport-matchs
  body:
    type: json
    data: |-
      {
          "player1Id": 1,
          "player2Id": 3,
          "matchDate": "2026-05-15"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
