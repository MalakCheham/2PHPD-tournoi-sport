info:
  name: Delete Tournament
  type: http
  seq: 9

http:
  method: DELETE
  url: http://127.0.0.1:8000/api/tournaments/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
