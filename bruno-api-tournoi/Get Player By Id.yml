info:
  name: Get Player By Id
  type: http
  seq: 11

http:
  method: GET
  url: http://127.0.0.1:8000/api/players/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
