info:
  name: Update Sport Match Score
  type: http
  seq: 23

http:
  method: PUT
  url: http://127.0.0.1:8000/api/tournaments/2/sport-matchs/2
  body:
    type: json
    data: |-
      {
          "scorePlayer1": 6,
          "scorePlayer2": 3
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
