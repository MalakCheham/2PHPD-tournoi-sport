info:
  name: Update Player
  type: http
  seq: 12

http:
  method: PUT
  url: http://127.0.0.1:8000/api/players/1
  body:
    type: json
    data: |-
      {
          "firstName": "Johnny",
          "status": "actif"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
