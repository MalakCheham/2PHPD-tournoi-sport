info:
  name: "Update Tournament "
  type: http
  seq: 8

http:
  method: PUT
  url: http://127.0.0.1:8000/api/tournaments/1
  body:
    type: json
    data: |-
      {
          "tournamentName": "Tournoi de Tennis Modifié",
          "location": "Lyon"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
