info:
  name: Get Sport Match By Id
  type: http
  seq: 22

http:
  method: GET
  url: http://127.0.0.1:8000/api/tournaments/2/sport-matchs/2
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
