info:
  name: Get Sport Matchs
  type: http
  seq: 18

http:
  method: GET
  url: http://127.0.0.1:8000/api/tournaments/2/sport-matchs
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
