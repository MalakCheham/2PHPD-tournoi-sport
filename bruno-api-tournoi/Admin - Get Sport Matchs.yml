info:
  name: Admin - Get Sport Matchs
  type: http
  seq: 27

http:
  method: GET
  url: http://127.0.0.1:8000/admin/sport-matchs
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
