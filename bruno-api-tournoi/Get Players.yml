info:
  name: Get Players
  type: http
  seq: 10

http:
  method: GET
  url: http://127.0.0.1:8000/api/players
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
