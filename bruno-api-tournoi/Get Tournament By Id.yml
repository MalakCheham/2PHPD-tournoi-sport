info:
  name: Get Tournament By Id
  type: http
  seq: 7

http:
  method: GET
  url: http://127.0.0.1:8000/api/tournaments/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
