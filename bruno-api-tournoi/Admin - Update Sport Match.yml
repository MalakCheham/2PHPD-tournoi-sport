info:
  name: Admin - Update Sport Match
  type: http
  seq: 28

http:
  method: PUT
  url: http://127.0.0.1:8000/admin/sport-matchs/1
  body:
    type: json
    data: |-
      {
          "scorePlayer1": 3,
          "scorePlayer2": 6
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
