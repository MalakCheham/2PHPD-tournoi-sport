info:
  name: Delete Sport Match
  type: http
  seq: 24

http:
  method: DELETE
  url: http://127.0.0.1:8000/api/tournaments/2/sport-matchs/2
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
